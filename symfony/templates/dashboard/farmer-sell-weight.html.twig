{% extends "base.html.twig" %} 
{% block body %}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h5">Sales Quantity by Product Weight (Kg)</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            {{include('dashboard/_main-form.html.twig')}}
            <div id='loader' style='background-color: rgba(0, 0, 0, 0.8);z-index: 999;position: absolute;left: 0;top: 0;width: 100%;height: 100%;display: none;' >
                {# <img src='{{ asset('images/Loading_icon2.gif') }}'> #}
            </div>
        </div>
    </div>
{{ render_chart(orders_time_line, {'id': 'orders_time_line','name' :'orders_time_line'}) }}

{% endblock %}  
  
